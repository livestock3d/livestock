language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt

script:
  - python -m pytest --cov-config .coveragerc --cov=livestock tests/

after_success:
  - coverage report
  - coveralls
  - mypy livestock
  - flake8 livestock